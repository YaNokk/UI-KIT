# External References

- `behavioral` — извлечённые API/behavioral-контракты Core DS;
- `visual` — извлечённые визуальные правила MP UI KIT;
- `raw` — необязательные исходные репозитории/снимки только для чтения.

В Iteration 0 внешние исходники не предоставлены. Их архитектура никогда не становится архитектурой проекта автоматически.

